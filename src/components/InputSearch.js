import React from "react";

// "<input class='border border-danger' placeholder='Search by Name' />"

// return JSX
const InputSearch = () => {
  return (
    <input
      className="border border-danger rounded"
      placeholder="Search by Location Name"
    />
  );
};

export default InputSearch;
